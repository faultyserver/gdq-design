import * as React from "react";

import UniImageQuestion from "@iconscout/react-unicons/icons/uil-image-question";
import { IconProps } from "./IconProps";

export default function Check(props: IconProps) {
  return <UniImageQuestion {...props} />;
}
